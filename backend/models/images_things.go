// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ImagesThing is an object representing the database table.
type ImagesThing struct {
	ThingID string `boil:"thing_id" json:"thing_id" toml:"thing_id" yaml:"thing_id"`
	ImageID string `boil:"image_id" json:"image_id" toml:"image_id" yaml:"image_id"`
	Pos     int    `boil:"pos" json:"pos" toml:"pos" yaml:"pos"`

	R *imagesThingR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L imagesThingL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ImagesThingColumns = struct {
	ThingID string
	ImageID string
	Pos     string
}{
	ThingID: "thing_id",
	ImageID: "image_id",
	Pos:     "pos",
}

var ImagesThingTableColumns = struct {
	ThingID string
	ImageID string
	Pos     string
}{
	ThingID: "images_things.thing_id",
	ImageID: "images_things.image_id",
	Pos:     "images_things.pos",
}

// Generated where

type whereHelperint struct{ field string }

func (w whereHelperint) EQ(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint) NEQ(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint) LT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint) LTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint) GT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint) GTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint) IN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint) NIN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var ImagesThingWhere = struct {
	ThingID whereHelperstring
	ImageID whereHelperstring
	Pos     whereHelperint
}{
	ThingID: whereHelperstring{field: "\"images_things\".\"thing_id\""},
	ImageID: whereHelperstring{field: "\"images_things\".\"image_id\""},
	Pos:     whereHelperint{field: "\"images_things\".\"pos\""},
}

// ImagesThingRels is where relationship names are stored.
var ImagesThingRels = struct {
	Image string
	Thing string
}{
	Image: "Image",
	Thing: "Thing",
}

// imagesThingR is where relationships are stored.
type imagesThingR struct {
	Image *Image `boil:"Image" json:"Image" toml:"Image" yaml:"Image"`
	Thing *Thing `boil:"Thing" json:"Thing" toml:"Thing" yaml:"Thing"`
}

// NewStruct creates a new relationship struct
func (*imagesThingR) NewStruct() *imagesThingR {
	return &imagesThingR{}
}

func (r *imagesThingR) GetImage() *Image {
	if r == nil {
		return nil
	}
	return r.Image
}

func (r *imagesThingR) GetThing() *Thing {
	if r == nil {
		return nil
	}
	return r.Thing
}

// imagesThingL is where Load methods for each relationship are stored.
type imagesThingL struct{}

var (
	imagesThingAllColumns            = []string{"thing_id", "image_id", "pos"}
	imagesThingColumnsWithoutDefault = []string{"thing_id", "image_id"}
	imagesThingColumnsWithDefault    = []string{"pos"}
	imagesThingPrimaryKeyColumns     = []string{"thing_id", "image_id"}
	imagesThingGeneratedColumns      = []string{}
)

type (
	// ImagesThingSlice is an alias for a slice of pointers to ImagesThing.
	// This should almost always be used instead of []ImagesThing.
	ImagesThingSlice []*ImagesThing
	// ImagesThingHook is the signature for custom ImagesThing hook methods
	ImagesThingHook func(context.Context, boil.ContextExecutor, *ImagesThing) error

	imagesThingQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	imagesThingType                 = reflect.TypeOf(&ImagesThing{})
	imagesThingMapping              = queries.MakeStructMapping(imagesThingType)
	imagesThingPrimaryKeyMapping, _ = queries.BindMapping(imagesThingType, imagesThingMapping, imagesThingPrimaryKeyColumns)
	imagesThingInsertCacheMut       sync.RWMutex
	imagesThingInsertCache          = make(map[string]insertCache)
	imagesThingUpdateCacheMut       sync.RWMutex
	imagesThingUpdateCache          = make(map[string]updateCache)
	imagesThingUpsertCacheMut       sync.RWMutex
	imagesThingUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var imagesThingAfterSelectMu sync.Mutex
var imagesThingAfterSelectHooks []ImagesThingHook

var imagesThingBeforeInsertMu sync.Mutex
var imagesThingBeforeInsertHooks []ImagesThingHook
var imagesThingAfterInsertMu sync.Mutex
var imagesThingAfterInsertHooks []ImagesThingHook

var imagesThingBeforeUpdateMu sync.Mutex
var imagesThingBeforeUpdateHooks []ImagesThingHook
var imagesThingAfterUpdateMu sync.Mutex
var imagesThingAfterUpdateHooks []ImagesThingHook

var imagesThingBeforeDeleteMu sync.Mutex
var imagesThingBeforeDeleteHooks []ImagesThingHook
var imagesThingAfterDeleteMu sync.Mutex
var imagesThingAfterDeleteHooks []ImagesThingHook

var imagesThingBeforeUpsertMu sync.Mutex
var imagesThingBeforeUpsertHooks []ImagesThingHook
var imagesThingAfterUpsertMu sync.Mutex
var imagesThingAfterUpsertHooks []ImagesThingHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ImagesThing) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ImagesThing) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ImagesThing) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ImagesThing) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ImagesThing) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ImagesThing) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ImagesThing) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ImagesThing) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ImagesThing) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range imagesThingAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddImagesThingHook registers your hook function for all future operations.
func AddImagesThingHook(hookPoint boil.HookPoint, imagesThingHook ImagesThingHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		imagesThingAfterSelectMu.Lock()
		imagesThingAfterSelectHooks = append(imagesThingAfterSelectHooks, imagesThingHook)
		imagesThingAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		imagesThingBeforeInsertMu.Lock()
		imagesThingBeforeInsertHooks = append(imagesThingBeforeInsertHooks, imagesThingHook)
		imagesThingBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		imagesThingAfterInsertMu.Lock()
		imagesThingAfterInsertHooks = append(imagesThingAfterInsertHooks, imagesThingHook)
		imagesThingAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		imagesThingBeforeUpdateMu.Lock()
		imagesThingBeforeUpdateHooks = append(imagesThingBeforeUpdateHooks, imagesThingHook)
		imagesThingBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		imagesThingAfterUpdateMu.Lock()
		imagesThingAfterUpdateHooks = append(imagesThingAfterUpdateHooks, imagesThingHook)
		imagesThingAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		imagesThingBeforeDeleteMu.Lock()
		imagesThingBeforeDeleteHooks = append(imagesThingBeforeDeleteHooks, imagesThingHook)
		imagesThingBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		imagesThingAfterDeleteMu.Lock()
		imagesThingAfterDeleteHooks = append(imagesThingAfterDeleteHooks, imagesThingHook)
		imagesThingAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		imagesThingBeforeUpsertMu.Lock()
		imagesThingBeforeUpsertHooks = append(imagesThingBeforeUpsertHooks, imagesThingHook)
		imagesThingBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		imagesThingAfterUpsertMu.Lock()
		imagesThingAfterUpsertHooks = append(imagesThingAfterUpsertHooks, imagesThingHook)
		imagesThingAfterUpsertMu.Unlock()
	}
}

// One returns a single imagesThing record from the query.
func (q imagesThingQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ImagesThing, error) {
	o := &ImagesThing{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for images_things")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ImagesThing records from the query.
func (q imagesThingQuery) All(ctx context.Context, exec boil.ContextExecutor) (ImagesThingSlice, error) {
	var o []*ImagesThing

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ImagesThing slice")
	}

	if len(imagesThingAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ImagesThing records in the query.
func (q imagesThingQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count images_things rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q imagesThingQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if images_things exists")
	}

	return count > 0, nil
}

// Image pointed to by the foreign key.
func (o *ImagesThing) Image(mods ...qm.QueryMod) imageQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ImageID),
	}

	queryMods = append(queryMods, mods...)

	return Images(queryMods...)
}

// Thing pointed to by the foreign key.
func (o *ImagesThing) Thing(mods ...qm.QueryMod) thingQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ThingID),
	}

	queryMods = append(queryMods, mods...)

	return Things(queryMods...)
}

// LoadImage allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (imagesThingL) LoadImage(ctx context.Context, e boil.ContextExecutor, singular bool, maybeImagesThing interface{}, mods queries.Applicator) error {
	var slice []*ImagesThing
	var object *ImagesThing

	if singular {
		var ok bool
		object, ok = maybeImagesThing.(*ImagesThing)
		if !ok {
			object = new(ImagesThing)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeImagesThing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeImagesThing))
			}
		}
	} else {
		s, ok := maybeImagesThing.(*[]*ImagesThing)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeImagesThing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeImagesThing))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &imagesThingR{}
		}
		args[object.ImageID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &imagesThingR{}
			}

			args[obj.ImageID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`images`),
		qm.WhereIn(`images.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Image")
	}

	var resultSlice []*Image
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Image")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for images")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for images")
	}

	if len(imageAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Image = foreign
		if foreign.R == nil {
			foreign.R = &imageR{}
		}
		foreign.R.ImagesThings = append(foreign.R.ImagesThings, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ImageID == foreign.ID {
				local.R.Image = foreign
				if foreign.R == nil {
					foreign.R = &imageR{}
				}
				foreign.R.ImagesThings = append(foreign.R.ImagesThings, local)
				break
			}
		}
	}

	return nil
}

// LoadThing allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (imagesThingL) LoadThing(ctx context.Context, e boil.ContextExecutor, singular bool, maybeImagesThing interface{}, mods queries.Applicator) error {
	var slice []*ImagesThing
	var object *ImagesThing

	if singular {
		var ok bool
		object, ok = maybeImagesThing.(*ImagesThing)
		if !ok {
			object = new(ImagesThing)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeImagesThing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeImagesThing))
			}
		}
	} else {
		s, ok := maybeImagesThing.(*[]*ImagesThing)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeImagesThing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeImagesThing))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &imagesThingR{}
		}
		args[object.ThingID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &imagesThingR{}
			}

			args[obj.ThingID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`things`),
		qm.WhereIn(`things.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Thing")
	}

	var resultSlice []*Thing
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Thing")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for things")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for things")
	}

	if len(thingAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Thing = foreign
		if foreign.R == nil {
			foreign.R = &thingR{}
		}
		foreign.R.ImagesThings = append(foreign.R.ImagesThings, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ThingID == foreign.ID {
				local.R.Thing = foreign
				if foreign.R == nil {
					foreign.R = &thingR{}
				}
				foreign.R.ImagesThings = append(foreign.R.ImagesThings, local)
				break
			}
		}
	}

	return nil
}

// SetImage of the imagesThing to the related item.
// Sets o.R.Image to related.
// Adds o to related.R.ImagesThings.
func (o *ImagesThing) SetImage(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Image) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"images_things\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"image_id"}),
		strmangle.WhereClause("\"", "\"", 2, imagesThingPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ThingID, o.ImageID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ImageID = related.ID
	if o.R == nil {
		o.R = &imagesThingR{
			Image: related,
		}
	} else {
		o.R.Image = related
	}

	if related.R == nil {
		related.R = &imageR{
			ImagesThings: ImagesThingSlice{o},
		}
	} else {
		related.R.ImagesThings = append(related.R.ImagesThings, o)
	}

	return nil
}

// SetThing of the imagesThing to the related item.
// Sets o.R.Thing to related.
// Adds o to related.R.ImagesThings.
func (o *ImagesThing) SetThing(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Thing) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"images_things\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"thing_id"}),
		strmangle.WhereClause("\"", "\"", 2, imagesThingPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ThingID, o.ImageID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ThingID = related.ID
	if o.R == nil {
		o.R = &imagesThingR{
			Thing: related,
		}
	} else {
		o.R.Thing = related
	}

	if related.R == nil {
		related.R = &thingR{
			ImagesThings: ImagesThingSlice{o},
		}
	} else {
		related.R.ImagesThings = append(related.R.ImagesThings, o)
	}

	return nil
}

// ImagesThings retrieves all the records using an executor.
func ImagesThings(mods ...qm.QueryMod) imagesThingQuery {
	mods = append(mods, qm.From("\"images_things\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"images_things\".*"})
	}

	return imagesThingQuery{q}
}

// FindImagesThing retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindImagesThing(ctx context.Context, exec boil.ContextExecutor, thingID string, imageID string, selectCols ...string) (*ImagesThing, error) {
	imagesThingObj := &ImagesThing{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"images_things\" where \"thing_id\"=$1 AND \"image_id\"=$2", sel,
	)

	q := queries.Raw(query, thingID, imageID)

	err := q.Bind(ctx, exec, imagesThingObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from images_things")
	}

	if err = imagesThingObj.doAfterSelectHooks(ctx, exec); err != nil {
		return imagesThingObj, err
	}

	return imagesThingObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ImagesThing) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no images_things provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(imagesThingColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	imagesThingInsertCacheMut.RLock()
	cache, cached := imagesThingInsertCache[key]
	imagesThingInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			imagesThingAllColumns,
			imagesThingColumnsWithDefault,
			imagesThingColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(imagesThingType, imagesThingMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(imagesThingType, imagesThingMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"images_things\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"images_things\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into images_things")
	}

	if !cached {
		imagesThingInsertCacheMut.Lock()
		imagesThingInsertCache[key] = cache
		imagesThingInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ImagesThing.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ImagesThing) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	imagesThingUpdateCacheMut.RLock()
	cache, cached := imagesThingUpdateCache[key]
	imagesThingUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			imagesThingAllColumns,
			imagesThingPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update images_things, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"images_things\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, imagesThingPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(imagesThingType, imagesThingMapping, append(wl, imagesThingPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update images_things row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for images_things")
	}

	if !cached {
		imagesThingUpdateCacheMut.Lock()
		imagesThingUpdateCache[key] = cache
		imagesThingUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q imagesThingQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for images_things")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for images_things")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ImagesThingSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), imagesThingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"images_things\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, imagesThingPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in imagesThing slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all imagesThing")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ImagesThing) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no images_things provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(imagesThingColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	imagesThingUpsertCacheMut.RLock()
	cache, cached := imagesThingUpsertCache[key]
	imagesThingUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			imagesThingAllColumns,
			imagesThingColumnsWithDefault,
			imagesThingColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			imagesThingAllColumns,
			imagesThingPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert images_things, could not build update column list")
		}

		ret := strmangle.SetComplement(imagesThingAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(imagesThingPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert images_things, could not build conflict column list")
			}

			conflict = make([]string, len(imagesThingPrimaryKeyColumns))
			copy(conflict, imagesThingPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"images_things\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(imagesThingType, imagesThingMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(imagesThingType, imagesThingMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert images_things")
	}

	if !cached {
		imagesThingUpsertCacheMut.Lock()
		imagesThingUpsertCache[key] = cache
		imagesThingUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ImagesThing record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ImagesThing) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ImagesThing provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), imagesThingPrimaryKeyMapping)
	sql := "DELETE FROM \"images_things\" WHERE \"thing_id\"=$1 AND \"image_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from images_things")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for images_things")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q imagesThingQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no imagesThingQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from images_things")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for images_things")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ImagesThingSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(imagesThingBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), imagesThingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"images_things\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, imagesThingPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from imagesThing slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for images_things")
	}

	if len(imagesThingAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ImagesThing) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindImagesThing(ctx, exec, o.ThingID, o.ImageID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ImagesThingSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ImagesThingSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), imagesThingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"images_things\".* FROM \"images_things\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, imagesThingPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ImagesThingSlice")
	}

	*o = slice

	return nil
}

// ImagesThingExists checks if the ImagesThing row exists.
func ImagesThingExists(ctx context.Context, exec boil.ContextExecutor, thingID string, imageID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"images_things\" where \"thing_id\"=$1 AND \"image_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, thingID, imageID)
	}
	row := exec.QueryRowContext(ctx, sql, thingID, imageID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if images_things exists")
	}

	return exists, nil
}

// Exists checks if the ImagesThing row exists.
func (o *ImagesThing) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ImagesThingExists(ctx, exec, o.ThingID, o.ImageID)
}
